%h2 New Category

= form_for [:admin, @category] do |f|
  = render 'error', model: @category
  .form-group
    = f.label :name
    = f.text_field :name, class: 'form-control'
  = f.submit class: 'btn btn-default'
