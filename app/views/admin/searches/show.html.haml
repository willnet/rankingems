%h1
  Search results for:
  = "'#{params[:name]}'"

%h2 Categories

%ul.list-group
  - @categories.each do |category|
    %li.list-group-item
      = link_to category.name, admin_rubygems_path(category)

%h2 Rubygems

%ul.list-group
  - @rubygems.each do |rubygem|
    %li.list-group-item
      = link_to rubygem.name, admin_rubygem_path(rubygem)
