%h1=@category.name

%ul.list-group
  - @rubygems.each do |rubygem|
    %li.list-group-item
      %h4.list-group-item-heading= link_to rubygem.name, rubygem
      Download count in 1 month
      = rubygem.download_count_for_ranking
