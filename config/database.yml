production:
  url:  <%= ENV["DATABASE_URL"] %>
  pool: 25

default: &default
  adapter: postgresql
  encoding: unicode

development:
  <<: *default
  database: savanna_development

test:
  <<: *default
  database: savanna_test
